public class Enum {
    public enum Actuator{
        light,coffee
    }
    /*public enum Manager{
        lightManager,coffeeManager
    }*/
    public enum Sensor{
        motion
    }
    public enum ActivationCondition{
        presence
    }
}
